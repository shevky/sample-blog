<footer class="border-t border-sky-500 px-6 py-4">
    <div class="flex flex-wrap justify-between gap-6">
        <div class="flex gap-3 flex-wrap content-start">
            {{#footer.tags}}
            <a href="{{{url}}}" class="tag-pill">{{label}}</a>
            {{/footer.tags}}
            {{^footer.tags}}
            <span class="text-sm text-slate-500 dark:text-slate-400">{{i18n.listing.empty}}</span>
            {{/footer.tags}}
        </div>

        <div class="flex flex-col gap-2 text-right">
            {{#footer.policies}}
            <a href="{{{url}}}"
                class="text-slate-600 dark:text-slate-400 hover:text-sky-600 dark:hover:text-sky-400 transition-colors">{{label}}</a>
            {{/footer.policies}}
        </div>
    </div>

    <script defer src="/output.js"></script>
    <script class="easter-egg">
        var data = {{{ easterEgg }}};
    </script>
</footer>
